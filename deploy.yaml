version: 1

components:
  - name: flask-app
    type: service
    image: python:3.11

    build:
      - pip install -r requirements.txt

    run:
      - gunicorn app:app --bind 0.0.0.0:8000

    ports:
      - 8000
